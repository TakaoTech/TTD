# This file was generated using Kotlin DSL (.github/workflows/ttd_build_workflow.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'Server build workflow'
on:
  push: { }
jobs:
  check_yaml_consistency:
    name: 'Check YAML consistency'
    runs-on: 'ubuntu-latest'
    steps:
      - id: 'step-0'
        name: 'Check out'
        uses: 'actions/checkout@v4'
      - id: 'step-1'
        name: 'Execute script'
        run: 'rm ''.github/workflows/ttd_build_workflow.yaml'' && ''.github/workflows/ttd_build_workflow.main.kts'''
      - id: 'step-2'
        name: 'Consistency check'
        run: 'git diff --exit-code ''.github/workflows/ttd_build_workflow.yaml'''
  build_server:
    runs-on: 'ubuntu-latest'
    needs:
      - 'check_yaml_consistency'
    steps:
      - id: 'step-0'
        name: 'Setup Java'
        uses: 'actions/setup-java@v4'
        with:
          java-version: '17'
          distribution: 'corretto'
      - id: 'step-1'
        name: 'Setup Gradle'
        uses: 'gradle/actions/setup-gradle@v3'
      - id: 'step-2'
        name: 'Checkout'
        uses: 'actions/checkout@v4'
      - id: 'step-3'
        name: 'Generate image'
        run: './gradlew server:publishImage'
